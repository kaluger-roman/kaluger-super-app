---
allowed-tools: Bash(ls:*), Bash(date:*), Bash(mkdir:*), Bash(rg:*), Bash(grep:*), Bash(find:*), Bash(curl:*), Bash(psql:*), Bash(awk:*), Bash(jot:*), Bash(shuf:*), Bash(echo:*), Bash(docker:*), Bash(bash scripts/qa-stack.sh:*), Bash(node:*), Bash(git:*), Read, Write, Glob, Grep, mcp__playwright__browser_navigate, mcp__playwright__browser_click, mcp__playwright__browser_type, mcp__playwright__browser_press_key, mcp__playwright__browser_take_screenshot, mcp__playwright__browser_snapshot, mcp__playwright__browser_wait_for, mcp__playwright__browser_console_messages, mcp__playwright__browser_network_requests, mcp__playwright__browser_evaluate, mcp__playwright__browser_close, mcp__playwright__browser_resize, mcp__playwright__browser_select_option, mcp__playwright__browser_hover, mcp__playwright__browser_fill_form, mcp__playwright__browser_handle_dialog, mcp__playwright__browser_tabs, mcp__playwright__browser_drag, mcp__playwright__browser_file_upload
description: QA/user-роуминг по живому приложению через Playwright MCP со случайными сценариями, скриншотами и MD-отчётом по всем подтверждённым улучшениям в docs/qa-roam-reports/
---

## Контекст

- Сегодня: !`date +%Y-%m-%d`
- Сид (для случайной выборки в этом запуске): !`date +%s`
- Существующие отчёты: !`ls -t docs/qa-roam-reports/ 2>/dev/null | head -5 || echo "(папка ещё не создана)"`
- QA-стек ветки: !`bash scripts/qa-stack.sh port 2>/dev/null || echo "не поднят — поднимется на Этапе 0"`
- Роуты приложения (autodiscovered): !`grep -E 'path="[^"]+"' frontend/src/app/components/AppRoutes/AppRoutes.tsx 2>/dev/null | grep -oE 'path="[^"]+"' | sort -u || echo "(не найдено — структура роутинга изменилась, см. Этап 1)"`

## Задача

Сыграть роль реального QA / пользователя / product owner'а: походить по живому приложению через Playwright MCP, потыкать в случайные сценарии (каждый запуск — разный набор), фиксировать на скриншотах что показалось странным/неудобным/неработающим, и записать **все подтверждённые** наблюдения, прошедшие фильтр Этапа 3, в `docs/qa-roam-reports/YYYY-MM-DD-qa-roam.md` (если файл с этой датой уже есть — суффикс `-2`, `-3`, …).

**Ограничения по сути:**

- Это **не bug-hunt** (тот ищет баги в коде) и **не improve-hunt** (тот читает код). Здесь — только то, что **видно/чувствуется при использовании** приложения как реальный пользователь.
- **Код НЕ менять**, тесты НЕ запускать, git-операций НЕ делать. Только отчёт + скриншоты.
- Каждый запуск должен давать **разный** результат: рандомизация выбора сценариев — обязательна (см. Этап 2).

`$ARGUMENTS` (можно комбинировать):
- пусто → случайно выбрать 2–3 роута из autodiscovered списка (фиксированный объём).
- `<path>` (например `/students` или `/lessons`) → роуминг только по этому роуту и тому, что с него достижимо.
- `--budget <N|N%>` → **режим токен-бюджета**: не ограничиваться 2–3 роутами, а крутить всё новые сценарии, пока есть бюджет токенов (см. секцию «Токен-бюджет»). Примеры: `--budget 20%` (20% от 5-часового окна подписки), `--budget 300k`, `--budget 250000`.
- `--host` → не поднимать Docker; роуминг по уже запущенным dev-серверам (`http://localhost:3000`).
- `--teardown` → в конце снести QA-стек (`bash scripts/qa-stack.sh down`). По умолчанию стек остаётся поднятым.

### Что считается «находкой»

| ✅ Репортить | ❌ Не репортить |
|---|---|
| Кнопка «Сохранить» висит disabled без объяснения, что не так с формой | Цвет primary чуть бледный (вкус) |
| После создания ученика список не обновляется до F5 | Заголовок страницы можно было бы написать иначе |
| 500-ка в network на странице расписания, у пользователя пустой экран | Не нравится анимация перехода |
| Удаление урока без подтверждения — кликнул мимо, данные пропали | Кнопка «Создать» могла бы быть FAB вместо обычной |
| Поле «Email» позволяет ввести «aaa» и сабмитить, ошибка только после ответа сервера | В консоли warning от React (не влияет на пользователя) |
| Скриншот: текст ошибки на английском («Internal Server Error») в RU-интерфейсе | Лишний `useEffect` в коде (это для improve-hunt) |
| На мобильном (resize 375px) сайдбар перекрывает контент | TypeScript-типы можно улучшить (это для improve-hunt) |

Ключевой тест: «Заметил бы это **реальный учитель**, использующий приложение?» Если да и это ухудшает опыт — репортить. Если это видно только при чтении кода — не сюда.

---

## Этап 0. Предполётная проверка

1. **Поднять изолированный QA-стек ветки** (кроме `--host`):

   ```bash
   bash scripts/qa-stack.sh up
   ```

   Печатает JSON `{"url":"http://localhost:<port>",...}` — распарсить `url` как **base URL** всего роуминга. Стек изолирован (своя свежая БД, эфемерный порт), поэтому несколько `/qa-roam` и `/manual-qa` на разных ветках идут параллельно. Первая сборка 2–4 мин. Если вернулся `{"error":...}` или Docker не запущен — сказать `Запусти Docker Desktop и перезапусти /qa-roam` и не идти дальше. Детали — `docs/qa-docker.md`. При `--host` base URL = `http://localhost:3000` (старое поведение).

2. **Playwright MCP доступен.** Сделать пробный `mcp__playwright__browser_navigate` на **base URL** (URL стека из шага 1; при `--host` — `http://localhost:3000`). Если возвращается ошибка отсутствия инструмента — сказать пользователю: `Playwright MCP не подключён. Подключи через "claude mcp add playwright npx @playwright/mcp@latest" и перезапусти /qa-roam.` Дальше не идти.

3. **Тестовый пользователь.** БД стека пустая. Проверить/создать `qa-roam@test.local` (пароль `QaRoam!2026`) — либо регистрацией **по UI** (это сам по себе первый journey, попутно покрывается), либо через `bash scripts/qa-stack.sh exec-backend '...'`. Быстрый просмотр: `bash scripts/qa-stack.sh psql "select email from users limit 5;"`. Все дальнейшие действия — под этим пользователем. (Админ-доступ к стеку, если нужен: `admin@qa.local` / `QaAdmin!2026`.)

4. **Тестовые данные.** БД свежая — создать минимальный набор: через API (`POST /api/students`, `POST /api/lessons`) под залогиненным пользователем, прямой вставкой через `exec-backend`, либо `bash scripts/qa-stack.sh exec-backend 'npm run db:seed'` (в изолированной БД безопасно). Ориентир: 3 ученика, 5 уроков (часть в прошлом, часть в будущем, часть recurring). Префикс `qa-roam-` для имён.

> Цель этапа — выйти из подготовки с залогиненным контекстом и предсказуемыми данными. На UI-логин/setup тратить максимум 5 минут — дальше идти через API/БД.

---

## Этап 1. Discovery роутов и случайный план обхода

**Никакого хардкод-списка областей в этой команде нет** — это сознательное решение, чтобы команду не приходилось обновлять при появлении новых страниц.

### 1.1. Получить актуальный список роутов

Источник правды — `frontend/src/app/components/AppRoutes/AppRoutes.tsx`. Распарсить все `path="..."` через `grep`:

```bash
grep -oE 'path="[^"]+"' frontend/src/app/components/AppRoutes/AppRoutes.tsx \
  | sed -E 's/path="(.+)"/\1/' \
  | grep -vE '^/$|/\*|^/login$|^/register$|^/verify-email|^/forgot-password|^/reset-password' \
  > /tmp/qa-roam-routes.txt
```

Фильтр `grep -vE` отбрасывает: служебный `/`, wildcards, чистый auth-flow (он покрывается на Этапе 0 при логине). Остаются «внутренние» роуты приложения.

**Если файл AppRoutes.tsx не нашёлся** (роутинг переехал) — fallback: взять имена директорий из `frontend/src/pages/` и попробовать угадать роут (`Reports` → `/reports`). Если и это не сработало — сказать пользователю и остановиться.

### 1.2. Случайно выбрать 2–3 роута из списка

Использовать сид из Контекста (`date +%s`), чтобы **разные запуски давали разный набор**:

```bash
SEED=$(date +%s)
N=$(( (SEED % 2) + 2 ))   # 2 или 3 роута
awk -v seed=$SEED 'BEGIN{srand(seed)} {print rand()"\t"$0}' /tmp/qa-roam-routes.txt \
  | sort | head -$N | cut -f2
```

Если `$ARGUMENTS` непустой — взять только указанный путь, без рандомизации количества.

### 1.3. Сценарии — генерировать на лету, не хардкодить

Для каждого выбранного роута сценарий **не предопределён**. На Этапе 2 ты:

1. Переходишь на роут.
2. Делаешь `browser_snapshot` (accessibility tree) — это покажет **актуально существующие** кнопки, формы, ссылки, списки.
3. Сам решаешь, что попробовать, исходя из увиденного и роли «реальный пользователь, который видит этот экран впервые». Без оглядки на старые версии экрана.

Примеры мыслительного процесса (ориентир, не чеклист):
- Видишь кнопку «Добавить ученика» → попробуй добавить.
- Видишь список → проверь пустое состояние (через фильтр без результатов или удаление всех записей), проверь длинный список (если есть скролл).
- Видишь форму → попробуй сабмит без обязательных полей, с edge-case значениями, нажми Enter.
- Видишь таблицу с действиями (редактировать/удалить) → попробуй удалить и отмени, проверь подтверждение.
- Видишь переключатели/табы → переключись несколько раз, проверь сохранение состояния при F5.

### 1.4. Случайный модификатор (универсальный, не зависит от фичи)

Выбрать **минимум один** модификатор для всего прохода — он применяется ко всем сценариям. Тоже через сид:

```bash
MODS="mobile-375 tablet-768 desktop edge-input-long edge-input-emoji edge-input-xss double-click keyboard-only"
SEED=$(date +%s)
echo $MODS | tr ' ' '\n' \
  | awk -v seed=$SEED 'BEGIN{srand(seed+1)} {print rand()"\t"$0}' \
  | sort | head -2 | cut -f2
```

Расшифровка:
- `mobile-375` / `tablet-768` / `desktop` — `mcp__playwright__browser_resize` на старте.
- `edge-input-long` — везде, где есть текстовый input, попробовать 500–1000 символов.
- `edge-input-emoji` — везде вставлять эмодзи (👨‍👩‍👧, проверка surrogate pairs).
- `edge-input-xss` — `<script>alert(1)</script>` в имена/описания (должно экранироваться).
- `double-click` — кнопки «Сохранить»/«Создать» кликать дважды подряд (защита от double-submit).
- `keyboard-only` — все формы заполнять и сабмитить **только** Tab + Enter, без мыши (проверяет focus order и Enter-submit).

### 1.5. Объявить план пользователю

Одной строкой **перед** началом обхода:

> План (seed=1715000000): обход роутов `/students`, `/lessons` с модификатором `mobile-375 + edge-input-emoji`.

---

## Токен-бюджет (режим `--budget`)

Если задан `--budget`, qa-roam **не ограничивается 2–3 роутами** — он крутит всё новые сценарии и сам докидывает идеи по ходу, пока есть бюджет токенов за текущее 5-часовое окно подписки.

1. **На старте** (сразу после подъёма стека) зафиксировать момент начала прогона и запомнить его как `RUN_START`:
   ```bash
   date -u +%Y-%m-%dT%H:%M:%SZ
   ```
2. **После каждого завершённого сценария** (пробы из Этапа 2) свериться с бюджетом:
   ```bash
   node scripts/qa-token-usage.mjs --since <RUN_START> --budget <spec>
   ```
   Скрипт печатает JSON со `spent` / `budgetTokens` / `percentOfBudgetUsed` / `verdict`. Знаменатель для `%` авто-детектится из истории сессий (максимальный 5-часовой блок); можно переопределить `--limit <N>` или env `QA_SESSION_TOKEN_LIMIT`. Метрика по умолчанию — `billed` (input+output+cache_creation, без дешёвых cache-read).
3. **`verdict == "stop"`** → завершить роуминг, перейти к фильтрации (Этап 3) и отчёту (Этап 4). **`verdict == "continue"`** → выбрать следующий сценарий (новый роут из autodiscovered-списка или более глубокий journey на текущем) и продолжить цикл.
4. Новые сценарии генерировать по принципам Этапа 1.3: смотри `browser_snapshot`, придумывай осмысленный путь реального пользователя. Идти **вширь** (новые роуты) и **вглубь** (edge-cases, негативные пути, realtime), не повторяя уже покрытое.

Оговорка: usage **текущего** хода ещё не сброшен на диск, поэтому замер слегка запаздывает (недооценка) — стоп срабатывает чуть раньше реального лимита, что безопасно. Без `--budget` бюджет не проверяется (обычный режим, 2–3 роута).

---

## Этап 2. Активный обход через Playwright MCP

Для каждого выбранного роута сделать 1–2 «пробы» (одна полноценная попытка пройти осмысленный путь — например создание сущности от и до). Для **каждой пробы**:

1. **Старт:** `browser_navigate` на стартовый URL области. Сделать `browser_snapshot` (accessibility tree) — это даст структуру страницы для умных кликов.
2. **Скриншот «до»:** `browser_take_screenshot` с понятным именем (например, `students-list-before.png`).
3. **Действия:** последовательность из 3–8 шагов (click/type/select), **наблюдая** результат каждого:
   - После каждого значимого действия — `browser_snapshot` или `browser_take_screenshot`.
   - Параллельно собирать `browser_console_messages` (искать `error`, `warning` уровня error) и `browser_network_requests` (искать `status >= 400`, очень долгие запросы, дубли).
4. **Проверки** (всё, что применимо):
   - **Видимый фидбэк:** после save — есть ли toast/snackbar/сообщение об успехе? Если нет — кандидат в находки.
   - **Loading state:** при долгом запросе — есть ли спиннер/скелетон? Если нет — кандидат.
   - **Empty state:** на странице со списком при отсутствии данных — что показано? Если просто пустота без объяснения — кандидат.
   - **Ошибки:** при сценарии с заведомо неверными данными — текст ошибки на русском? Понятный? Не «Internal Server Error»?
   - **Сохранение состояния:** после refresh — данные на месте? Фильтры? Авторизация?
   - **Навигация:** кнопка «назад» в браузере работает корректно? URL отражает состояние?
   - **a11y-минимум:** есть ли видимый focus при Tab? Можно ли отправить форму с Enter?
   - **Согласованность:** одинаково ли выглядят похожие элементы (кнопки, диалоги, иконки)?
5. **Скриншот «после»:** финальное состояние.
6. **Логирование наблюдений:** записать в рабочий список **всё, что показалось странным** — даже мелочи. Фильтрация будет на Этапе 3.

**Скриншоты** сохранять в `docs/qa-roam-reports/screenshots/YYYY-MM-DD-NNN-<имя>.png` (NNN — порядковый номер). Эти файлы потом упомянуты в отчёте.

### Бюджет

Жёсткой отсечки «10 действий на пробу» нет — это убило бы любой нетривиальный journey (recurring-серия, forgot-password-цикл, отчёт-с-фильтрами легко требуют 15–25 шагов). Вместо этого:

- **На пробу:** идти до естественного конца journey (сохранил / убедился, что не получилось / упёрся в баг). Если в районе **25 действий** ещё не видно края — это сигнал «застрял», свернуть пробу и записать наблюдение «journey слишком длинный/непонятный для нового пользователя» (это сама по себе находка).
- **На всю команду:** в обычном режиме — потолок ~60 действий браузера и ~25 скриншотов на запуск. В режиме `--budget` потолка по числу действий/скриншотов нет: продолжай новые сценарии, пока `node scripts/qa-token-usage.mjs` не вернёт `verdict: stop` (см. «Токен-бюджет»); скриншоты при этом снимай так же щедро.
- **Скриншоты** — снимать щедро, не экономить: каждое «не так» снимать сразу (потом не вернуться к тому же состоянию), плюс кадр «до» и «после» для каждой пробы. Это дешёвый эвиденс — лучше иметь лишний, чем потерять.

### Шаблон промпта для себя (внутренний)

Перед каждой пробой проговорить вслух (в сообщении пользователю, одной строкой):

> Проба на `/students` (мод. mobile-375 + emoji): попробую добавить ученика с именем «Иван 👨‍👩‍👧 Петров» и посмотрю, как он отрисуется в списке.

После пробы — короткая (1–2 строки) сводка:

> Готово (12 действий). Наблюдения: (1) нет фидбэка после save; (2) эмодзи в списке заменился на «?».

---

## Этап 3. Финальная фильтрация и приоритизация

Собрать все наблюдения с Этапа 2. Применить фильтры:

1. **Это пользовательское?** Если видно только в DevTools и не влияет на UX (например, deprecation warning React) — отбросить или объединить с другим.
2. **Это уже известно?** Быстро проверить `docs/bug-reports/`, `docs/improvement-reports/`, `docs/qa-roam-reports/` за последние 30 дней (Glob + Grep по заголовку). Если уже репортилось — пропустить.
3. **Это не вкусовщина?** Если суть «мне бы хотелось чтобы кнопка была круглой» — отбросить.
4. **Это не новая фича?** Тест: «Если убрать — функциональность исчезнет?» Да → фича, отбросить (для фич — отдельный канал).
5. **Дубликаты** между сценариями — слить в один пункт с примерами из разных областей.

Оставшееся отсортировать по приоритету (по матрице ниже) — но **писать в отчёт всё**, что прошло фильтры. Жёсткой верхней границы по количеству нет: десять мелких UX-шероховатостей и две High-Impact-багги — это десять плюс две записи, а не «топ-5».

| Impact | Effort | Приоритет |
|---|---|---|
| High | S | 1 (Quick Win) |
| High | M | 2 |
| High | L | 3 |
| Medium | S | 4 |
| Medium | M/L | 5 |
| Low | * | 6 (в самый низ, но всё равно в отчёте, если прошло фильтры) |

**Impact-критерий:** сколько % пользователей это заденет × как сильно (потеря данных / непонимание / небольшое раздражение).

---

## Этап 4. Отчёт

1. `mkdir -p docs/qa-roam-reports/screenshots` (если папок ещё нет).
2. Имя файла: `docs/qa-roam-reports/YYYY-MM-DD-qa-roam.md` (если занято — `-2`, `-3`, …).
3. Записать через Write tool.

**Структура отчёта:**

```markdown
# QA Roam Report — YYYY-MM-DD

**Seed:** 1715000000
**Покрытые роуты:** `/students`, `/lessons`
**Модификатор:** mobile-375 + edge-input-emoji
**Действий выполнено:** ~38 / **Скриншотов:** 18
**Токен-бюджет:** выключен | напр.: 20% → 2,994,405 ток.; потрачено 2,8M (94%); остановлен после 7 сценариев
**Всего наблюдений до фильтрации:** N → в отчёте: M (все, что прошло фильтры Этапа 3)

## Резюме

2–3 предложения о главных болях, которые увидел «пользователь» в этом проходе. Топ — что бы сам учитель назвал самым неудобным.

---

## 1. [Заголовок улучшения, как сказал бы пользователь]

- **Impact:** High | Medium
- **Effort:** S (≤2 ч) | M (полдня) | L (день+)
- **Категория:** UX / Visible Bug / Copy / Mobile / a11y / Performance (visible)
- **Где:** `URL: /students` или `Экран: «Создание урока»`
- **Скриншот:** `screenshots/2026-05-19-001-students-no-feedback.png`

### Что увидел

Реальный сценарий, через который проходил (3–5 шагов). Чем закончился. Что было «не так» с точки зрения пользователя.

### Почему это проблема

Что почувствует пользователь: запутается / потеряет данные / не поймёт, что произошло / решит, что приложение сломано / попробует ещё раз и сделает дубль.

### Что предложить

Конкретное предложение (без кода — это уровень дизайна/UX, не реализации):
- «Показывать snackbar `Ученик создан` на 3 секунды»
- «Disabled-кнопка должна объяснять, что нужно для активации (через title или текст под кнопкой)»

### Воспроизведение

```
1. Логин под qa-roam@test.local
2. /students → «Добавить»
3. Заполнить имя, нажать «Сохранить»
4. (Наблюдение) Кнопка моргнула, но никакого визуального подтверждения нет
5. Только если перейти в список — увидишь нового
```

---

## 2. ...
```

---

## Ограничения

- **Без верхнего лимита** по количеству пунктов. Включай всё, что прошло фильтры Этапа 3 (не вкусовщина, не известное, не код-уровень). Сортируй по приоритету.
- **Каждый пункт — со скриншотом**, путь относительно репозитория. Скриншоты лежат рядом в `screenshots/`.
- **Без кода**. Это отчёт о пользовательском опыте, а не PR.
- **Только то, что видел сам в этом запуске.** Не репортить «по памяти» из других проходов.
- **Описания на русском**, как говорил бы реальный пользователь («не понятно, что делать», «думал, что сломалось»).
- **Не редактировать прод-код, не запускать тесты, не делать git-операций.**
- **Изолированная БД стека.** Префикс `qa-roam-` для создаваемого (для удобства), но чистить за собой не обязательно — стек сносится `bash scripts/qa-stack.sh down` (или флагом `--teardown`). Личную БД пользователя не трогать — она не используется.
- **Docker-стек по умолчанию остаётся поднятым** после прогона (для ручной до-проверки) — печатать его URL в финале. При `--host` стек не поднимается.
- В финальном сообщении пользователю — путь к отчёту, seed, выбранные области, URL стека, и одна строка-сводка: `N улучшений (High X, Medium Y, Low Z); Quick Wins: <короткие заголовки>`. В режиме `--budget` добавить: потрачено/бюджет токенов (`%`) и сколько сценариев пройдено до остановки.
